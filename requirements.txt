# --- Temel Veri ve Sayısal Hesaplama ---
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# --- Makine Öğrenmesi ---
scikit-learn>=1.2.0           # Tox21 modeli ve final scorer için

# --- Biyoinformatik ve Genetik Analiz ---
biopython>=1.80               # DNA/RNA sekans analizi, pairwise alignment

# --- İlaç Tasarımı ve Kimyasal Bilişim ---
rdkit-pypi                    # Moleküler analiz, SMILES okuma ve ilaç tasarımı
                              # NOT: 'rdkit' değil 'rdkit-pypi' pip ile kurulur

# --- Moleküler Docking (Opsiyonel) ---
# meeko                       # PDBQT ligand hazırlama (pip install meeko)
# vina                        # AutoDock Vina Python API (pip install vina)

# --- Görselleştirme ---
matplotlib>=3.7.0
seaborn>=0.12.0

# --- API ve Altyapı ---
requests                      # PDB/Tox21 veri indirme
tqdm                          # İlerleme çubuğu

# --- Opsiyonel Geliştirme Araçları ---
# jupyterlab                  # İnteraktif notebook ortamı
# plotly                      # İnteraktif grafikler
# py3Dmol                     # 3D molekül görselleştirme
